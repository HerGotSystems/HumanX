name = "humanx"
main = "src/worker.js"
compatibility_date = "2026-05-24"

assets = { directory = "./public", binding = "ASSETS" }

# D1 database is optional for the current lightweight AIP-first deploy.
# Add DB binding later in Cloudflare UI or replace this file with a real database_id.
