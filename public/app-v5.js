const API='';
const LS_USER='humanx_public_user_v1';
let claims=[],evidenceVault=[],truths=[],selected=null,mode='arena',lastPacket='',live=false,user=null;
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function toast(t){const e=document.createElement('div');e.className='toast';e.textContent=t;document.body.appendChild(e);setTimeout(()=>e.remove(),1800)}
function headers(){return {'content-type':'application/json','x-humanx-user':user?.id||''}}
async function api(path,opts={}){const r=await fetch(API+path,{...opts,headers:{...headers(),...(opts.headers||{})}});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||data.error||'Request failed');return data}
function localUser(){let u=JSON.parse(localStorage.getItem(LS_USER)||'null');if(!u){u={id:'usr_'+crypto.randomUUID().replaceAll('-','').slice(0,18),handle:'anon-'+Math.random().toString(36).slice(2,8)};localStorage.setItem(LS_USER,JSON.stringify(u))}return u}
function sourceLink(url){if(!url)return'';const safe=esc(url);return`<p class="small source"><a href="${safe}" target="_blank" rel="noopener noreferrer">${safe}</a></p>`}
function cleanStance(v){v=String(v||'support').toLowerCase().trim();return v==='pressure'?'pressure':'support'}
async function boot(){user=localUser();document.getElementById('who').textContent=user.handle;try{const h=await api('/api/health');live=h.mode==='d1-live';setStatus(live?'D1 live shared arena':'Demo fallback — D1 not connected yet',live);try{const s=await api('/api/session',{method:'POST',body:JSON.stringify(user)});if(s.user){user={...user,...s.user};document.getElementById('who').textContent=user.handle;localStorage.setItem(LS_USER,JSON.stringify(user))}}catch{}await loadClaims()}catch(e){setStatus('Backend unreachable',false,true);renderError(e)}}
function setStatus(t,ok,bad=false){document.getElementById('status').textContent=t;const d=document.getElementById('dot');d.className='dot '+(bad?'bad':ok?'live':'')}
async function loadClaims(){try{const q=encodeURIComponent(document.getElementById('search')?.value||'');const f=encodeURIComponent(document.getElementById('filter')?.value||'all');const data=await api(`/api/claims?q=${q}&status=${f}`);claims=data.claims||[];if(data.warning)setStatus(data.warning,false);render()}catch(e){renderError(e)}}
async function loadEvidenceVault(){const q=encodeURIComponent(document.getElementById('search')?.value||'');const data=await api(`/api/evidence-vault?q=${q}`);evidenceVault=data.evidence||[];}
async function loadTruths(){const q=encodeURIComponent(document.getElementById('search')?.value||'');const data=await api(`/api/truths?q=${q}`);truths=data.truths||[];}
function setMode(m){mode=m;selected=null;document.body.classList.remove('study-mode');document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));document.getElementById('tab-'+m)?.classList.add('active');render()}
function cls(s){if(s==='Proven'||String(s).includes('Supported'))return'b-green';if(String(s).includes('Disproven')||String(s).includes('Collapse'))return'b-red';if(s==='Plausible')return'b-blue';return'b-yellow'}
function meter(n,v){v=Math.max(0,Math.min(100,Number(v||0)));return`<div class="meter"><span>${n}</span><div class="bar"><div class="fill" style="width:${v}%"></div></div></div>`}
function render(){if(mode==='submit')return renderSubmit();if(mode==='vault')return renderVault();if(mode==='truths')return renderTruths();if(mode==='export')return renderExport();renderArena()}
function renderArena(){document.body.classList.remove('study-mode');document.getElementById('casefile').innerHTML='<p class="small">Select a claim to open full study mode.</p>';document.getElementById('main').innerHTML=`<div class="grid">${claims.map(c=>card(c,true)).join('')||'<div class="panel">No claims found.</div>'}</div>`}
function card(c,actions=false){return`<article class="card"><span class="badge ${cls(c.status)}">${esc(c.status)}</span><h3>${esc(c.claim)}</h3><p class="small">${esc(c.category)} · ${esc(c.type)} · by ${esc(c.handle||'anon')}</p><div class="meters">${meter('Evidence',c.evidenceScore)}${meter('Test',c.testability)}${meter('Survive',c.survivability)}</div><p class="small"><b>${esc(c.contradictions||0)}</b> pressure points · belief ${esc(c.beliefYes||0)} / reject ${esc(c.beliefNo||0)} / unsure ${esc(c.uncertainty||0)}</p>${actions?`<div class="actions"><button class="primary" onclick="selectClaim('${esc(c.id)}')">Study Claim</button></div>`:''}</article>`}
function renderSubmit(){document.body.classList.remove('study-mode');document.getElementById('casefile').innerHTML='<p class="small">Submit a claim first. Full case file appears after inspection.</p>';document.getElementById('main').innerHTML=`<div class="panel"><h2>Submit Public Claim</h2><p class="small">Pseudonymous only. No email. Add evidence where possible. Claims without evidence stay weak until defended.</p><div class="claimbox"><input id="cClaim" placeholder="Claim"><input id="cCategory" placeholder="Category, e.g. Health, Politics, History, Physics"><select id="cType"><option>Physical/Testable</option><option>Historical</option><option>Religious/Belief</option><option>Conspiracy</option><option>Product Claim</option><option>Medical/High Risk</option></select><textarea id="cEvidence" placeholder="Initial evidence or reason"></textarea><button class="primary" onclick="saveClaim()">Submit to HumanX</button></div></div>`}
async function renderVault(){document.body.classList.remove('study-mode');document.getElementById('casefile').innerHTML='<p class="small">Evidence Vault stores reusable evidence. One document, video, dataset or test can support or pressure multiple claims later.</p>';document.getElementById('main').innerHTML='<div class="panel"><h2>Loading Evidence Vault…</h2></div>';try{await loadEvidenceVault();document.getElementById('main').innerHTML=`<div class="panel"><h2>Evidence Vault</h2><p class="small">Reusable evidence objects. These are not claims; they are materials that can be linked to claims as support or pressure.</p></div><div class="grid evidence-grid">${evidenceVault.map(e=>evidenceCard(e)).join('')||'<div class="panel">No evidence found.</div>'}</div>`}catch(e){renderError(e)}}
function evidenceCard(e){return`<article class="card evidence-card"><span class="badge b-blue">${esc(e.quality)} · ${esc(e.mediaType||'text')}</span><h3>${esc(e.title)}</h3><p class="small">Reliability ${esc(e.reliabilityScore||'?')} · stance ${esc(e.stance||'support')} · by ${esc(e.handle||'anon')}</p><p>${esc(e.body)}</p>${sourceLink(e.sourceUrl)}<div class="row"><span class="pill">linked claim</span><p class="small">${esc(e.claim||'No linked claim')}</p><span class="badge ${cls(e.claimStatus)}">${esc(e.claimStatus||'unknown')}</span></div><div class="actions">${e.claimId?`<button onclick="selectClaim('${esc(e.claimId)}')">Study Linked Claim</button>`:''}<button class="primary" onclick="attachEvidencePrompt('${esc(e.id)}')">Attach to Another Claim</button></div></article>`}
async function renderTruths(){document.body.classList.remove('study-mode');document.getElementById('casefile').innerHTML='<p class="small">Truths are repeated statements, slogans, doctrines and inherited certainties. Repetition is measured separately from proof.</p>';document.getElementById('main').innerHTML='<div class="panel"><h2>Loading Truths…</h2></div>';try{await loadTruths();document.getElementById('main').innerHTML=`<div class="panel"><h2>Truth Statements</h2><p class="small">A truth statement is not automatically true. HumanX tracks repetition, origin and pressure before turning statements into testable claims.</p><div class="claimbox"><input id="truthStatement" placeholder="Truth statement, slogan, doctrine or inherited certainty"><input id="truthCategory" placeholder="Category, e.g. religion, politics, family, science, culture"><input id="truthOrigin" placeholder="Origin, e.g. church, school, internet, family, government"><select id="truthType"><option value="common">Common truth</option><option value="religious">Religious truth</option><option value="political">Political truth</option><option value="scientific">Scientific consensus</option><option value="family">Family truth</option><option value="cultural">Cultural truth</option></select><button class="primary" onclick="submitTruth()">Add Truth Statement</button></div></div><div class="grid truth-grid">${truths.map(t=>truthCard(t)).join('')||'<div class="panel">No truth statements yet.</div>'}</div>`}catch(e){renderError(e)}}
function truthCard(t){return`<article class="card truth-card"><span class="badge b-yellow">${esc(t.truthType||'truth')} · ${esc(t.confidenceLabel||'claimed')}</span><h3>${esc(t.statement)}</h3><p class="small">Category ${esc(t.category||'general')} · origin ${esc(t.origin||'unknown')} · by ${esc(t.handle||'anon')}</p><div class="meters"><div class="meter"><span>Repeated</span><b>${esc(t.repetitionScore||1)}</b></div><div class="meter"><span>Pressure</span><b>${esc(t.pressureScore||0)}</b></div><div class="meter"><span>Claim Link</span><b>${t.linkedClaimId?'yes':'no'}</b></div></div><p class="small">This may be belief, doctrine, assumption or slogan. It becomes stronger only when converted into a claim and tested.</p><div class="actions"><button class="primary" onclick="convertTruth('${esc(t.id)}')">Convert to Claim</button></div></article>`}
async function attachEvidencePrompt(evidenceId){if(!claims.length)await loadClaims();const options=claims.map((c,i)=>`${i+1}. ${c.claim}`).join('\n');const choice=prompt('Choose target claim number:\n\n'+options);if(!choice)return;const target=claims[Number(choice)-1];if(!target){toast('Invalid claim choice');return;}const stance=cleanStance(prompt('Use as support or pressure?', 'support')||'support');try{await api('/api/evidence-attach',{method:'POST',body:JSON.stringify({evidenceId,claimId:target.id,stance})});toast(`Evidence attached as ${stance} to: ${target.claim}`)}catch(e){toast(e.message||'Could not attach evidence')}}
async function submitTruth(){const statement=document.getElementById('truthStatement')?.value||'';const category=document.getElementById('truthCategory')?.value||'general';const origin=document.getElementById('truthOrigin')?.value||'unknown';const truthType=document.getElementById('truthType')?.value||'common';try{await api('/api/truths',{method:'POST',body:JSON.stringify({statement,category,origin,truthType})});toast('Truth recorded');await renderTruths()}catch(e){toast(e.message)}}
function renderExport(){document.body.classList.remove('study-mode');document.getElementById('casefile').innerHTML='<p class="small">AIP packets are generated from selected claim case files.</p>';document.getElementById('main').innerHTML=`<div class="panel"><h2>AIP-first Export</h2><p class="small">HumanX does not spend owner API credits on public analysis. Generate a packet, copy it, and run it inside any AI account/model.</p><div class="actions"><button class="primary" onclick="generateAIP()">Generate selected claim packet</button><button onclick="downloadJSON()">Download visible claims JSON</button></div><pre class="output">${esc(JSON.stringify({user,claims,evidenceVault,truths},null,2))}</pre></div>`}
async function selectClaim(id){try{const data=await api('/api/claims/'+id);selected=data.claim;selected.evidence=data.evidence||[];selected.pressure=data.pressure||[];selected.tests=data.tests||data.home_tests||[];renderStudy();toast('Study mode loaded')}catch(e){selected=claims.find(c=>c.id===id)||selected;renderStudy();toast(e.message)}}
function renderStudy(){if(!selected)return;document.body.classList.add('study-mode');document.getElementById('main').innerHTML=`<div class="study"><button onclick="backToArena()">← Back to claims</button><span class="badge ${cls(selected.status)}">${esc(selected.status)}</span><h2>${esc(selected.claim)}</h2><p class="small">${esc(selected.category)} · ${esc(selected.type)} · by ${esc(selected.handle||'anon')}</p><div class="meters wide">${meter('Evidence',selected.evidenceScore)}${meter('Testability',selected.testability)}${meter('Survivability',selected.survivability)}</div><div class="actions"><button class="vote yes" onclick="voteClaim('believe')">Believe</button><button class="vote no" onclick="voteClaim('reject')">Reject</button><button class="vote unsure" onclick="voteClaim('unsure')">Unsure</button></div><p class="small">Belief is not truth. Votes show human confidence, not proof.</p><div class="study-grid"><section>${sectionEvidence()}</section><section>${sectionPressure()}</section><section>${sectionTests()}</section></div></div>`;renderCaseMini()}
function renderCaseMini(){document.getElementById('casefile').innerHTML=`<h3>Quick Add</h3><p class="small">Selected: ${esc(selected.claim)}</p><div class="actions"><button onclick="generateAIP()">AIP Packet</button><button class="danger" onclick="reportSelected()">Report</button></div>`}
function evidenceMeta(e){const reused=e.link_type==='reused';const stance=e.linked_stance||e.stance||'support';return`${reused?'REUSED · ':''}${esc(e.quality||e.stance)} · ${esc(e.media_type||e.mediaType||'text')} · ${esc(stance)} · reliability ${esc(e.reliability_score||e.reliabilityScore||'?')}`}
function sectionEvidence(){return`<h3>Evidence / Defence</h3>${(selected.evidence||[]).map(e=>`<div class="row"><span class="pill">${evidenceMeta(e)}</span><b>${esc(e.title)}</b><p class="small">${esc(e.body||e.note)}</p>${e.link_note?`<p class="small"><b>Reuse note:</b> ${esc(e.link_note)}</p>`:''}${sourceLink(e.source_url||e.sourceUrl)}</div>`).join('')||'<p class="small">No loaded evidence yet.</p>'}`}
function sectionPressure(){return`<h3>Pressure / Attack</h3>${(selected.pressure||[]).map(p=>`<div class="row"><span class="pill">severity ${esc(p.severity||1)}</span><b>${esc(p.title)}</b><p class="small">${esc(p.body||p.note)}</p></div>`).join('')||'<p class="small">No loaded pressure yet.</p>'}`}
function sectionTests(){return`<h3>Home / Repeatability Tests</h3>${(selected.tests||[]).map(t=>`<div class="row"><span class="pill">${esc(t.difficulty||'test')} · ${esc(t.safety_level||t.safetyLevel||'normal')}</span><b>${esc(t.title)}</b><p class="small">${esc(t.instructions)}</p></div>`).join('')||'<p class="small">No tests loaded yet.</p>'}`}
function backToArena(){document.body.classList.remove('study-mode');selected=null;renderArena()}
async function voteClaim(vote){if(!selected)return toast('Select a claim first');try{const data=await api('/api/claim-vote',{method:'POST',body:JSON.stringify({claimId:selected.id,vote})});selected=data.claim||selected;toast('Vote recorded');await loadClaims();await selectClaim(selected.id)}catch(e){toast(e.message||'Vote route not wired yet')}}
async function saveClaim(){const body={claim:document.getElementById('cClaim').value,category:document.getElementById('cCategory').value,type:document.getElementById('cType').value,initialEvidence:document.getElementById('cEvidence').value};try{const data=await api('/api/claims',{method:'POST',body:JSON.stringify(body)});selected=data.claim;toast('Claim submitted');mode='arena';await loadClaims();await selectClaim(selected.id)}catch(e){toast(e.message)}}
async function addCaseItem(){if(!selected)return toast('Select a claim first');const kind=document.getElementById('eKind').value;const payload={claimId:selected.id,title:document.getElementById('eTitle').value,quality:document.getElementById('eQuality').value,body:document.getElementById('eNote').value,sourceUrl:document.getElementById('eSource')?.value||''};try{const data=await api(kind==='evidence'?'/api/evidence':'/api/pressure',{method:'POST',body:JSON.stringify(payload)});selected=data.claim||selected;await selectClaim(selected.id);document.getElementById('eTitle').value='';document.getElementById('eNote').value='';if(document.getElementById('eSource'))document.getElementById('eSource').value='';toast(kind==='evidence'?'Evidence attached':'Pressure attached')}catch(e){toast(e.message)}}
function quickPressure(id){selectClaim(id).then(()=>{document.getElementById('eKind').value='pressure';document.getElementById('eTitle').focus()})}
function quickEvidence(id){selectClaim(id).then(()=>{document.getElementById('eKind').value='evidence';document.getElementById('eTitle').focus()})}
async function reportSelected(){if(!selected)return toast('Select a claim first');const reason=prompt('Why report this claim? Spam, doxxing, illegal content, harassment, scam, etc.');if(!reason)return;try{await api('/api/report',{method:'POST',body:JSON.stringify({targetType:'claim',targetId:selected.id,reason})});toast('Report sent for admin review')}catch(e){toast(e.message)}}
async function convertTruth(truthId){try{const data=await api('/api/truth-to-claim',{method:'POST',body:JSON.stringify({truthId})});toast('Truth converted to claim');mode='arena';await loadClaims();if(data.bridge?.claimId)await selectClaim(data.bridge.claimId)}catch(e){toast(e.message||'Could not convert truth')}}
async function generateAIP(){if(!selected)return toast('Select a claim first');try{const data=await api('/api/aip',{method:'POST',body:JSON.stringify({claimId:selected.id})});lastPacket=JSON.stringify(data.packet,null,2)}catch(e){lastPacket=JSON.stringify({aip_version:'1.1',app:'HumanX',mode:'claim-pressure-analysis',no_owner_api_used:true,instruction:'Analyse this claim. Public HumanX AI calls are disabled; user runs this in their own AI.',payload:selected},null,2)}document.getElementById('aip').textContent=lastPacket;toast('AIP packet ready')}
async function copyAIP(){if(!lastPacket)await generateAIP();navigator.clipboard.writeText(lastPacket);toast('Copied')}
function downloadJSON(){const blob=new Blob([JSON.stringify({user,claims,evidenceVault,truths},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='humanx-visible-data.json';a.click();URL.revokeObjectURL(a.href)}
function renderError(e){document.getElementById('main').innerHTML=`<div class="panel"><h2>HumanX backend notice</h2><p class="small">${esc(e.message||e)}</p></div>`}
window.setMode=setMode;window.loadClaims=loadClaims;window.selectClaim=selectClaim;window.quickPressure=quickPressure;window.quickEvidence=quickEvidence;window.saveClaim=saveClaim;window.addCaseItem=addCaseItem;window.reportSelected=reportSelected;window.generateAIP=generateAIP;window.copyAIP=copyAIP;window.downloadJSON=downloadJSON;window.voteClaim=voteClaim;window.backToArena=backToArena;window.submitTruth=submitTruth;window.convertTruth=convertTruth;window.attachEvidencePrompt=attachEvidencePrompt;
boot();
